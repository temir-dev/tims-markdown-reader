# Images and navigation

A document can include a local image alongside the text. This checkerboard is a tiny project-owned test image.

![A checkerboard used to check local image rendering](checker.png)

## Things to try

- Make the window smaller and larger. The image should stay within the reading area.
- Choose Serif or Monospaced in Settings. The image itself should remain unchanged.
- Follow [this link to the final section](#the-final-section), then scroll back up.
- Open [the prose sample](01-reading.md), then use **⌘[** to return.

## Missing image

The following image intentionally does not exist. Its absence should be handled without breaking the page.

![This test image is intentionally missing](intentionally-missing.png)

## Literal HTML

This reader displays raw HTML as text rather than turning it into an interactive page:

<button>Example button from a Markdown file</button>

## The final section

You made it to the heading target. [Back to the sample list](00-start-here.md).
